mutation postRelease(
  $artistId: Int!
  $type: String!
  $title: String!
  $released: String!
  $language: String!
  $genres: [String]!
  $tracks: [String]!
  $cover: String!
) {
  postRelease(
    artistId: $artistId
    type: $type
    title: $title
    released: $released
    language: $language
    genres: $genres
    tracks: $tracks
    cover: $cover
  ) {
    id
  }
}
