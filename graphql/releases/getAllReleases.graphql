query getAllReleases($take: Int!, $skip: Int!) {
  getAllReleases(take: $take, skip: $skip) {
    id
    genres
    artistId
    title
    rating
    released
    ratingCount
    cover
  }
  hasMore
}
